module.exports = {
     post: async (settings, data) => {
        if(settings.discordbotsorg === "true" || settings.discordbotsgg === "true") {
        
        } else {
          console.log("Nothing to post")
        }
     },

};
